<h1> All Players </h1>

<ul>
    <% @players.each do |player| %>
        <li><%= link_to player.name , player_path(player) %></li>
        <%end %>
</ul>

<h3><%= link_to "All Basketball Teams", basketball_teams_path %></h3>